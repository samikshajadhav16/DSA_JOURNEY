# Write your MySQL query statement below
select p.product_id , IFNULL(ROUND(sum(u.units*p.price)/sum(u.units),2),0) as average_price from Prices p Left join UnitsSold u on p.product_id=u.product_id and u.purchase_date BETWEEN p.start_date and p.end_date group by(product_id);